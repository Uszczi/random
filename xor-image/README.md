# Simple xor encryption for images.

Small app for playing with basic PyQt.

## Instalation

`pip install -r requirements.txt`

`pip install -e .`

## Usage

`python-xor --help`

`python-xor gui`

`python-xor cli IMAGE_PATH KEY_PATH PATH_TO_RESULT`
